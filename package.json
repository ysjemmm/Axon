{
  "name": "axon-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Axon —— 品牌化 AI IDE：同一 Agent 内核驱动 Web 与 Code OSS 两种形态",
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
